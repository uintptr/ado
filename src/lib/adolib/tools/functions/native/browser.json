[
    {
        "name": "browse",
        "description": "Open a URL in the default browser.",
        "inputSchema": {
            "type": "object",
            "properties": {
                "url": {
                    "type": "string",
                    "description": "URL to open.",
                    "format": "uri"
                }
            },
            "required": ["url"],
            "additionalProperties": false
        }
    }
]
