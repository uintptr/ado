[
    {
        "type": "function",
        "name": "shell_exec",
        "description": "function to execute any shell command on the host",
        "parameters": {
            "type": "object",
            "properties": {
                "command_line": {
                    "type": "string",
                    "description": "command line to execute, The output data will be json data base64 encoded"
                }
            },
            "required": [
                "command_line"
            ],
            "additionalProperties": false
        }
    }
]